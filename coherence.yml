backend:
  dev:
    - npm
    - run
    - dev
  prod:
    - npm
    - run
    - start
  
  migration:  ["npm", "run","migrate","up"]
  seed: ["npm", "run","migrate","up"]

  type: backend
  url_path: /api
  resources:
    - name: db1
      type: database
      engine: postgres
      version: 14
  local_packages:
    - node_modules
frontend:
  type: backend
  url_path: /
  repo_path: .
  
  local_packages: ["node_modules"]
  install: ["npm", "install"]
  build: ["npm", "run", "build"]
  dev: ["npm", "run", "dev"]
  prod:
     - npm
     - run
     - start
